# 复制为 api/.env（本地开发）或仓库根目录 .env（docker compose），填入真实值；.env 不入库

# DeepSeek API Key（必填：LLM 判题/对话考官/实操窗口/报告建议均依赖）
DEEPSEEK_API_KEY=

# 可选：自建网关或代理地址（默认官方 https://api.deepseek.com）
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# 可选：判题与对话模型（默认 deepseek-v4-pro / deepseek-flash 双模型分工）
# DEEPSEEK_MODEL_JUDGE=deepseek-v4-pro
# DEEPSEEK_MODEL_CHAT=deepseek-flash

# JWT 签名密钥：生产部署必须换成随机长字符串（保持默认值启动时打 WARNING）
# COMPASS_JWT_SECRET=dev-secret-change-me

# CORS 白名单：逗号分隔多个来源（默认 http://localhost:5173）
# COMPASS_CORS_ORIGINS=http://localhost:5173

# 注：SQLite 路径 COMPASS_DB 只认进程环境变量（不读本文件），docker compose 已设为 /data/compass.db
